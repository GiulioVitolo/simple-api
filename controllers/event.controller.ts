import { Request, Response, NextFunction } from "express";

export function addEvent(req: Request, res: Response, next: NextFunction) {

}

export function getEvents(req: Request, res: Response, next: NextFunction) {
  
}

export function getEvent(req: Request, res: Response, next: NextFunction) {
  
}
